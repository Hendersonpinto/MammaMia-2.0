<div id="mom-card" data-toggle="modal" data-target="#myModal<%= mom.name %>">
  <div id="owner-avatar">
    <!-- PICTURE OF USER -->
  </div>

  <div id="back-card">
    <div id="mom-picture-card">
      <!-- PICTURE OF MOM -->
      <img id="mom-picture" src="https://source.unsplash.com/random/500x300" alt="<%= mom.name %>">
      <h2 id="mom-picture-card-name"><%= mom.name %></h2>
    </div>
    <div id="bottom-card">
      <p><%= mom.owner.name %>'s mom (DESCRIPTION)</p>
    </div>
  </div>
</div>
